FROM nginx:alpine
COPY . /usr/share/nginx/html/
EXPOSE 80
```

### 4. Guardar
- Scroll hacia abajo
- Click en **"Commit new file"**

## Resumen de lo que tendrás en tu repo:
```
bustosmg/mbustos/
├── index.html (o tu archivo HTML actual)
└── Dockerfile (el que acabas de crear)
